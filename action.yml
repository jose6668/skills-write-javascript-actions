name: "Joke Action"
description: "Fetches a random joke and exposes it as an output"
# Minimal metadata for the GitHub Skills dad joke action.

outputs:
  joke:
    description: "The fetched joke text"

runs:
  using: node24
  main: dist/index.js
